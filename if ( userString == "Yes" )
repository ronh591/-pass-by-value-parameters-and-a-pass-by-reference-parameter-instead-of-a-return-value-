#include <iostream>
#include <string>
#include <sstream>
using namespace std;
int main()
{
  
  string userString;
  //grab the string
  cout << "type a word: ";
  getline(cin, userString);
  if (userString == "yes" || userString == "Yes")
    cout << "you typed an acceptable form of \"yes\".";
  else 
    {cout << "you did not type";
    cout << "an acceptable form of \"yes\".";}
}
